apply plugin: 'java'

sourceSets {
	main {
        java {
            srcDir 'src'
        }
    }
    test {
        java {
            srcDir 'test'
        }
    }
}

dependencies {
    testCompile fileTree(dir: 'test-libs', include: '*.jar')
}